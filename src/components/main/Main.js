import SearchContainer from '../search/SearchContainer';
import './Main.css';

const Main = () => {
	return (
		<main className="main">
			<SearchContainer />
		</main>
	)
}

export default Main;